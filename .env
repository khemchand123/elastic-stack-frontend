# ElasticStack MCP Configuration

# Webhook Endpoint (Frontend calls this directly for now, unless you want that wrapped too)
NEXT_PUBLIC_MCP_WEBHOOK_URL=https://imworkflow.intermesh.net/webhook/es_mcp

# The wrapper API endpoint the frontend will call (relative path)
NEXT_PUBLIC_INDEX_LIST_API_URL=/api/indices

# The ACTUAL Backend Elastic Endpoint (Server-side use only)
# This is what the wrapper API will call
ELASTIC_BACKEND_URL=http://10.84.85.37:8081/_cat/indices
